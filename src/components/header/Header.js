import Nav from "./Nav";
import MainSection from "./MainSection";
import FeaturedIn from "./FeaturedIn";

const Header = () => {
  return (
    <header>
      <Nav />
      <MainSection />
      <FeaturedIn />
    </header>
  );
};

export default Header;
